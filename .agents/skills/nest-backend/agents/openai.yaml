interface:
  display_name: "Nest Backend"
  short_description: "Repo-specific patterns and workflows for nest-backend"
  default_prompt: "Use the nest-backend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true