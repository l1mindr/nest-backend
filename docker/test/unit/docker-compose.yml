services:
  app:
    build:
      context: ../../..
      target: test

    environment:
      NODE_ENV: test

      ACCESS_TOKEN_SECRET: accessjwtsecret
      Refresh_TOKEN_SECRET: refreshjwtsecret

    command: pnpm run test:unit
